export type Recipe = {
  name: string;
  image: string;
  origin: string;
  category: string;
  source: string;
  youtube: string;
  instructions: string;
  ingredients: string[];
  measurements: string[];
}

